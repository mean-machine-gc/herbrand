{
  "name": "herbrand-mcp",
  "version": "0.2.1",
  "description": "Herbrand MCP server — decision-first business analysis for information systems",
  "type": "module",
  "bin": {
    "herbrand-mcp": "./dist/index.js"
  },
  "files": [
    "dist/",
    "src/skills/"
  ],
  "scripts": {
    "start": "npx tsx src/index.ts",
    "bundle": "tsup && chmod +x dist/index.js && cp -r src/skills dist/skills",
    "typecheck": "tsc --noEmit",
    "prepublishOnly": "npm run bundle"
  },
  "dependencies": {
    "fastmcp": "^3.0.0"
  },
  "devDependencies": {
    "@herbrand/core": "*",
    "@types/node": "^22.0.0",
    "tsup": "^8.5.1",
    "tsx": "^4.19.0",
    "typescript": "^5.9.3"
  },
  "keywords": ["herbrand", "mcp", "business-analysis", "decision-modelling", "cqrs", "event-sourcing"],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/mean-machine-gc/herbrand"
  }
}
